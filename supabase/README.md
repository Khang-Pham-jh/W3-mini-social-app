This folder stores tracked Supabase database setup files for the project.

Structure:
- `schema/`: SQL that defines required tables, policies, and triggers.

Current setup:
- `schema/profiles.sql`: creates and secures the `public.profiles` table used by the app.

Apply this SQL to your Supabase project before testing profile sync features.
